[phases.setup]
nixPkgs = ["python311", "nodejs20", "npm", "tesseract", "poppler_utils"]

[phases.install]
cmds = ["npm install", "pip install -r requirements.txt"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
