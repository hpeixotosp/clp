# Arquivo de exemplo para variáveis de ambiente de produção
# Copie este arquivo para .env.production no servidor e ajuste os valores

# Ambiente de execução
NODE_ENV=production

# Porta onde o Next.js irá rodar
PORT=3000

# URL base da API (através do Nginx)
NEXT_PUBLIC_API_URL=http://143.110.196.243/api

# URL do backend (mesmo que API_URL para consistência)
NEXT_PUBLIC_BACKEND_URL=http://143.110.196.243/api

# Ambiente público (usado para condicionais no frontend)
NEXT_PUBLIC_ENVIRONMENT=production

# URL base da aplicação (usado para SEO e meta tags)
NEXT_PUBLIC_APP_URL=http://143.110.196.243

# Configurações de build do Next.js
NEXT_TELEMETRY_DISABLED=1

# Configurações de segurança
# Descomente e configure se necessário
# NEXTAUTH_SECRET=your-secret-key-here
# NEXTAUTH_URL=http://143.110.196.243

# Configurações de banco de dados (se o frontend precisar)
# DATABASE_URL=postgresql://user:password@localhost:5432/clp_db

# Configurações de cache (se usar Redis)
# REDIS_URL=redis://localhost:6379

# Configurações de logging
# LOG_LEVEL=info

# Configurações de monitoramento (se usar Sentry, etc.)
# SENTRY_DSN=your-sentry-dsn-here
# SENTRY_ENVIRONMENT=production

# Configurações de analytics (se usar Google Analytics, etc.)
# NEXT_PUBLIC_GA_ID=your-google-analytics-id

# Configurações de CDN (se usar)
# NEXT_PUBLIC_CDN_URL=https://your-cdn-url.com

# Configurações específicas da aplicação CLP
# Ajuste conforme necessário para sua aplicação

# Timeout para requisições da API (em milissegundos)
NEXT_PUBLIC_API_TIMEOUT=30000

# Número máximo de itens por página
NEXT_PUBLIC_ITEMS_PER_PAGE=20

# Configurações de upload de arquivos
NEXT_PUBLIC_MAX_FILE_SIZE=10485760  # 10MB em bytes
NEXT_PUBLIC_ALLOWED_FILE_TYPES=pdf,doc,docx,xls,xlsx,jpg,jpeg,png

# Configurações de sessão
NEXT_PUBLIC_SESSION_TIMEOUT=3600000  # 1 hora em milissegundos